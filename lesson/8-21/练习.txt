-- SELECT  dept_no,d_name ,count(dept_no) as dept_count
-- FROM  dept INNER JOIN em
-- on d_no =dept_no GROUP BY d_name;

-- SELECT   d_name,AVG(e_salary) as dept_avg,em.dept_no
-- FROM dept INNER JOIN em
-- on d_no = dept_no GROUP BY d_name;

--  select d_name,d_location
--  FROM dept INNER JOIN em
--  on d_no=dept_no and e_name = 'jones';

-- SELECT *from dept INNER JOIN em
-- on d_no=dept_no and d_location='shenzhen';

-- SELECT * FROM em
-- where e_salary  in ( SELECT  max(e_salary) from dept INNER JOIN em
-- on d_no = dept_no GROUP BY d_name);